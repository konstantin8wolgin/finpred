#!/usr/bin/env bash
# Render real-vs-generated sample candlestick PNGs across training checkpoints.
# STUB (Phase B): for each checkpoint in checkpoints/<run_id>/ (e.g. step_0, step_25000,
# step_50000), generate a few fakes + pull a few real held-out windows, render them side by
# side via finpred.render.ohlcv_to_png into reports/<run_id>/samples/, so you can see the
# generator's visual progression.
set -euo pipefail
echo "render_samples.sh — implement in Phase B" >&2
exit 1
